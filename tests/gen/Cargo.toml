[package]
name = "tests-generator"
version = "0.1.0"
edition = "2021"

[dependencies]
reqwest = { version = "0.11.12", features = ["blocking"] }
zip-extract = "0.1.1"
gbemu-core = { path =  "../../lib/core" }
tests-shared = { path = "../shared" }
