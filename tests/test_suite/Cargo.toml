[package]
name = "testrunner"
version = "0.1.0"
edition = "2021"
build = "build.rs"

[dependencies]
gemi-core = { path = "../../lib/core" }
tests-shared = { path = "../shared" }

[build-dependencies]
tests-generator = { path = "../gen" }
