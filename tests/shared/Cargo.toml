[package]
name = "tests-shared"
version = "0.1.0"
edition = "2021"

[dependencies]
image = "0.25.0"
reqwest = { version = "0.11.12", features = ["blocking"] }
zip-extract = "0.1.1"
gemi-core = { path =  "../../lib/core" }
